ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-haiku-4-5

# Where traces are sent. Give the bare host — the collector resolves the
# transport and path itself. Unset means tracing goes nowhere, silently.
PHOENIX_COLLECTOR_ENDPOINT=http://localhost:6006
PHOENIX_PROJECT_NAME=arize-fde-travel-agent
